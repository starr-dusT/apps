image:
  repository: tccr.io/truecharts/chevereto
  pullPolicy: IfNotPresent
  tag: v1.5.1

securityContext:
  runAsNonRoot: false

PodSecurityPolicy:
  runAsUser: 0
  runAsGroup: 0

env:
  PUID: 568

service:
  main:
    ports:
      main:
        targetPort: 80
        port: 80

persistence:
  varrun:
    enabled: true
